# ContactManager
This web application automatically syncs Google contacts into a MySQL database and displays them in a searchable, responsive Tailwind dashboard. It enables single-click client-side PDF/Excel exports alongside direct WhatsApp and mobile call buttons for every contact.
